import React from "react";
import Layout from "../components/layout";

const About = () => {
    return (
        <Layout pageTitle="About">
            <div>This is where our about page will go</div>
        </Layout>
    );
};

export default About;
